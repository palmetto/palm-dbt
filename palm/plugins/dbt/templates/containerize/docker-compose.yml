version: "3.5"
services:
  {{project_name}}:
    image: {{project_name}}
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/app
    env_file:
      - .env
    environment:
      - DBT_PROFILES_DIR=/app
    # TODO: fix hardcoded port
    ports: 
      - "8080:8080"